{
  "name": "pulsehood",
  "version": "0.1.0",
  "private": true,
  "description": "PulseHood V1 - Robinhood Chain momentum radar and paper trading platform",
  "type": "module",
  "scripts": {
    "build": "node scripts/fix-token-labels.mjs && node scripts/fix-v4-market.mjs && node scripts/fix-v4-types.mjs && tsc --noEmit",
    "test": "node --test --import tsx src/**/*.test.ts",
    "dev": "tsx src/bootstrap.ts",
    "start": "tsx src/bootstrap.ts"
  },
  "dependencies": {
    "viem": "^2.37.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
