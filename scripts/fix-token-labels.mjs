import fs from 'node:fs';

const path = new URL('../src/index.ts', import.meta.url);
let s = fs.readFileSync(path, 'utf8');

s = s.replace(
"const ERC20_SYMBOL = [{ type: 'function', name: 'symbol', stateMutability: 'view', inputs: [], outputs: [{ type: 'string' }] }] as const;",
"const ERC20_SYMBOL = [{ type: 'function', name: 'symbol', stateMutability: 'view', inputs: [], outputs: [{ type: 'string' }] }, { type: 'function', name: 'name', stateMutability: 'view', inputs: [], outputs: [{ type: 'string' }] }] as const;"
);
s = s.replace(
"const symbolCache=new Map<string,string>();\nconst symbolRetryAt=new Map<string,number>();\nconst SYMBOL_RETRY_MS=30_000;",
"const symbolCache=new Map<string,string>();\nconst nameCache=new Map<string,string>();\nconst symbolRetryAt=new Map<string,number>();\nconst SYMBOL_RETRY_MS=30_000;\nconst fallbackTokenLabel=(address:string)=>address.length>14?`${address.slice(0,8)}…${address.slice(-6)}`:address;"
);
const old = `async function tokenSymbol(address:string):Promise<string>{\n  const key=address.toLowerCase();\n  const cached=symbolCache.get(key);\n  if(cached) return cached;\n  const retryAt=symbolRetryAt.get(key)??0;\n  if(Date.now()<retryAt) return 'TOKEN';\n  symbolRetryAt.set(key,Date.now()+SYMBOL_RETRY_MS);\n  try{\n    const symbol=await rpc.getClient().readContract({address:address as \`0x\${string}\`,abi:ERC20_SYMBOL,functionName:'symbol'}) as string;\n    const clean=String(symbol??'').trim();\n    if(clean){symbolCache.set(key,clean);symbolRetryAt.delete(key);return clean;}\n  }catch{}\n  return 'TOKEN';\n}`;
const replacement = `async function tokenSymbol(address:string):Promise<string>{\n  const key=address.toLowerCase();\n  const cachedName=nameCache.get(key);\n  if(cachedName) return cachedName;\n  const cached=symbolCache.get(key);\n  if(cached) return cached;\n  const retryAt=symbolRetryAt.get(key)??0;\n  if(Date.now()<retryAt) return fallbackTokenLabel(address);\n  symbolRetryAt.set(key,Date.now()+SYMBOL_RETRY_MS);\n  try{\n    const client=rpc.getClient();\n    const [symbolResult,nameResult]=await Promise.allSettled([\n      client.readContract({address:key as \`0x\${string}\`,abi:ERC20_SYMBOL,functionName:'symbol'}),\n      client.readContract({address:key as \`0x\${string}\`,abi:ERC20_SYMBOL,functionName:'name'})\n    ]);\n    const symbol=symbolResult.status==='fulfilled'?String(symbolResult.value??'').trim():'';\n    const name=nameResult.status==='fulfilled'?String(nameResult.value??'').trim():'';\n    if(name){nameCache.set(key,name);if(symbol)symbolCache.set(key,symbol);symbolRetryAt.delete(key);return name;}\n    if(symbol){symbolCache.set(key,symbol);symbolRetryAt.delete(key);return symbol;}\n  }catch{}\n  return fallbackTokenLabel(address);\n}`;
if(s.includes(old)) s=s.replace(old,replacement);
s=s.replace("symbol:symbolCache.get(x.token.toLowerCase())??'TOKEN'", "symbol:nameCache.get(x.token.toLowerCase())??symbolCache.get(x.token.toLowerCase())??fallbackTokenLabel(x.token),tokenName:nameCache.get(x.token.toLowerCase())??null");
s=s.replace("symbol: s.symbol&&String(s.symbol)!==token.slice(0,8)?s.symbol:await tokenSymbol(token)", "symbol: s.symbol&&String(s.symbol)!==token.slice(0,8)&&String(s.symbol)!=='TOKEN'?s.symbol:await tokenSymbol(token)");
s=s.replace(".r{display:grid;grid-template-columns:1.55fr .85fr .7fr .8fr .8fr .75fr .8fr .85fr;gap:8px;align-items:center;", ".r{display:grid;grid-template-columns:1.55fr .85fr .7fr .8fr .8fr .75fr .8fr .85fr;gap:8px;align-items:center;min-width:0;");
s=s.replace(".table .r{min-width:900px}", ".table .r{min-width:900px}.r>span{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.r>span>b{display:block;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}");

const runtimePath = new URL('../src/trading-runtime.ts', import.meta.url);
let r = fs.readFileSync(runtimePath, 'utf8');
r = r.replace("import { parseAbi } from 'viem';", "import { decodeEventLog, parseAbi } from 'viem';");
r = r.replace("const V3 = parseAbi(['function slot0() view returns (uint160 sqrtPriceX96, int24 tick, uint16 observationIndex, uint16 observationCardinality, uint16 observationCardinalityNext, uint8 feeProtocol, bool unlocked)', 'function liquidity() view returns (uint128)', 'function token0() view returns (address)', 'function token1() view returns (address)']);", "const V3 = parseAbi(['function slot0() view returns (uint160 sqrtPriceX96, int24 tick, uint16 observationIndex, uint16 observationCardinality, uint16 observationCardinalityNext, uint8 feeProtocol, bool unlocked)', 'function liquidity() view returns (uint128)', 'function token0() view returns (address)', 'function token1() view returns (address)']);\nconst V4_SWAP = parseAbi(['event Swap(bytes32 indexed id,address indexed sender,int128 amount0,int128 amount1,uint160 sqrtPriceX96,uint128 liquidity,int24 tick,uint24 fee)']);\nconst V4_STATE_VIEW = parseAbi(['function getSlot0(bytes32 poolId) view returns (uint160 sqrtPriceX96,int24 tick,uint24 protocolFee,uint24 lpFee)','function getLiquidity(bytes32 poolId) view returns (uint128 liquidity)']);\nconst V4_STATE_VIEW_ADDRESS = '0xf3334192d15450cdd385c8b70e03f9a6bd9e673b'.toLowerCase() as Addr;");
r = r.replace("const request = protocol === 'uniswap-v3' ? this.readV3Market(pool, target, quote) : this.readV2Market(pool, target, quote);", "const request = protocol === 'uniswap-v4' ? this.readV4Market(pool, target, quote, key) : protocol === 'uniswap-v3' ? this.readV3Market(pool, target, quote) : this.readV2Market(pool, target, quote);");
r = r.replace("private async readV2Market(pool: Addr, target: Addr, quote: Addr | undefined): Promise<PoolMarket | undefined> {", "private async readV4Market(pool: Addr, target: Addr, quote: Addr | undefined, cacheKey: string): Promise<PoolMarket | undefined> {\n    if (quote === undefined) return undefined;\n    try {\n      const parts = cacheKey.split(':');\n      const poolId = parts.length >= 5 ? parts[parts.length - 1] : undefined;\n      if (poolId === undefined || !/^0x[0-9a-fA-F]{64}$/.test(poolId)) return undefined;\n      const [slot0, liquidity] = await Promise.all([\n        this.client.readContract({ address: V4_STATE_VIEW_ADDRESS, abi: V4_STATE_VIEW, functionName: 'getSlot0', args: [poolId as `0x${string}`] }),\n        this.client.readContract({ address: V4_STATE_VIEW_ADDRESS, abi: V4_STATE_VIEW, functionName: 'getLiquidity', args: [poolId as `0x${string}`] }),\n      ]);\n      const sqrtPriceX96 = (slot0 as readonly [bigint, number, number, number])[0];\n      const L = Number(liquidity as bigint);\n      const pRaw = Number(sqrtPriceX96) ** 2 / 2 ** 192;\n      if (!Number.isFinite(pRaw) || pRaw <= 0 || !Number.isFinite(L) || L <= 0) return undefined;\n      const targetIs0 = await this.v4TargetIs0(poolId as `0x${string}`, target, quote);\n      if (targetIs0 === undefined) return undefined;\n      const targetDecimals = Number(await this.client.readContract({ address: target, abi: ERC20, functionName: 'decimals' }));\n      const quoteDecimals = Number(await this.client.readContract({ address: quote, abi: ERC20, functionName: 'decimals' }));\n      const quoteUsd = normalizedQuotePrice(quote);\n      const priceUsd = targetIs0 ? pRaw * 10 ** (targetDecimals - quoteDecimals) * quoteUsd : (1 / pRaw) * 10 ** (quoteDecimals - targetDecimals) * quoteUsd;\n      if (!Number.isFinite(priceUsd) || priceUsd <= 0) return undefined;\n      const sqrtP = Number(sqrtPriceX96) / 2 ** 96;\n      const virtual0 = L / sqrtP / 10 ** targetDecimals;\n      const virtual1 = L * sqrtP / 10 ** quoteDecimals;\n      const liqUsd = targetIs0 ? (virtual0 * priceUsd + virtual1 * quoteUsd) : (virtual0 * quoteUsd + virtual1 * priceUsd);\n      return { liquidityUsd: finite(liqUsd), priceUsd: finite(priceUsd), quotePriceUsd: quoteUsd };\n    } catch { return undefined; }\n  }\n\n  private async v4TargetIs0(poolId: `0x${string}`, target: Addr, quote: Addr): Promise<boolean | undefined> {\n    try {\n      const receipt = await this.client.getLogs({ event: V4_SWAP[0] as never, fromBlock: this.lastBlock > 200n ? this.lastBlock - 200n : 0n, toBlock: this.lastBlock, address: '0x8366a39cc670b4001a1121b8f6a443a643e40951' as Addr } as never);\n      for (const log of receipt.slice().reverse()) {\n        try {\n          const d = decodeEventLog({ abi: V4_SWAP, topics: log.topics, data: log.data });\n          const a = d.args as Record<string, unknown>;\n          if (String(a.id).toLowerCase() === poolId.toLowerCase()) {\n            const amount0 = BigInt(a.amount0 as bigint), amount1 = BigInt(a.amount1 as bigint);\n            if (amount0 === 0n || amount1 === 0n) continue;\n            return amount0 < 0n ? target.toLowerCase() !== quote.toLowerCase() && true : target.toLowerCase() !== quote.toLowerCase() && false;\n          }\n        } catch {}\n      }\n    } catch {}\n    return undefined;\n  }\n\n  private async readV2Market(pool: Addr, target: Addr, quote: Addr | undefined): Promise<PoolMarket | undefined> {");

// Make V4 pool id available in the normalized swap used by the strategy.
const radarPath = new URL('../src/radar-ingest.ts', import.meta.url);
let g = fs.readFileSync(radarPath, 'utf8');
g = g.replace("pool:V4_MANAGER!,transactionHash:log.transactionHash,", "pool:V4_MANAGER!,poolId:id as `0x${string}`,transactionHash:log.transactionHash,");
fs.writeFileSync(radarPath, g);
fs.writeFileSync(runtimePath, r);
fs.writeFileSync(path, s);
console.log(JSON.stringify({event:'token_label_patch',tokenFallback:'contract-address',nameLookup:true,namePreferred:true,overlapGuard:true,v4MarketStateView:true,v4PoolId:true,source:'src/index.ts'}));
